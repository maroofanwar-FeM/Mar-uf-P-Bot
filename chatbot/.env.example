# Copy this file to ".env" (same folder) and set your own login credentials.
# The server reads this file on startup. It is never sent to the browser.
APP_USERNAME=your-username-here
APP_PASSWORD=your-password-here

# Free API key for the "Run" feature's replies — get one at console.groq.com/keys
GROQ_API_KEY=your-groq-api-key-here

# Optional: override the model (defaults to openai/gpt-oss-120b if unset)
# GROQ_MODEL=openai/gpt-oss-120b

# Postgres connection string for the literature-chatbot database (Supabase).
# Supabase dashboard -> Connect -> Direct Connection -> copy the URI, then
# substitute in your database password for [YOUR-PASSWORD].
DATABASE_URL=
